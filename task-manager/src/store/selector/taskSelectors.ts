// // src/features/task/taskSelectors.ts
// // import { TaskStatus } from '../../interfaces';
// import { RootState } from '../../store'; // tu archivo de store
// import { TaskStatus } from '../slices';
// // import { TaskState } from './taskSlice';

// export const getTaskByStatus = (status: TaskStatus) =>
//   Object.values(state.task.tasks).filter(task => task.status === status);

// export const getTotalTask = (state: RootState) => Object.keys(state.task.tasks).length;
